Deploy the contents over the existing repository while preserving the current folder structure.

The three new source files are already located inside the documents folder. Do not move them to the website root.

This is an incremental update. Existing live-site documents, images and other assets not included in this ZIP must remain in place.
