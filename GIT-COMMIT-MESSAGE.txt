feat(seo): add canonical metadata and structured data across repository

- add canonical URLs to 22 public repository pages
- add index/follow search-engine directives
- add Open Graph URL, type and site-name metadata
- add Twitter summary card metadata
- add Schema.org WebPage and WebSite structured data
- refresh robots.txt and sitemap.xml
- retain existing content, filenames, links and repository structure
- exclude obsolete methodology page from indexing
