chore(release): reconcile repository status to v8.1.0

Update all supplied HTML pages and root configuration files to the common
repository release v8.1.0 dated 30 July 2026.

Reconcile JSON-LD release metadata, version values, dateModified fields,
visible record-version footers and sitemap lastmod dates. Add the current
repository release entry to the Record Status page while preserving earlier
documentary version history, page content, stable URLs and page-specific
development identifiers.
